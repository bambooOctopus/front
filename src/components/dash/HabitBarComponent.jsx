export const HabitBar = ({currentContent, setCurrentContent}) => {   
    return (                   
        <div className="habit-bar">                                           
            <p>habit bar</p>                    
        </div>        
    )
}